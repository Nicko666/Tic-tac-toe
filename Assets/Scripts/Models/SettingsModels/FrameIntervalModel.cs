public struct FrameIntervalModel
{
    public readonly int Interval;
    public readonly int FramesCount;

    public FrameIntervalModel(int interval, int framesCount)
    {
        Interval = interval;
        FramesCount = framesCount;
    }
}
